# 让 Git 按文件类型统一行尾
*          text=auto

# 代码/配置统一用 LF（跨平台更友好）
*.java     text eol=lf
*.xml      text eol=lf
*.properties text eol=lf
*.yml      text eol=lf
*.yaml     text eol=lf
*.md       text eol=lf
*.sh       text eol=lf

# Windows 脚本保留 CRLF
*.bat      text eol=crlf
*.cmd      text eol=crlf
*.ps1      text eol=crlf

# 二进制文件（不做行尾转换）
*.jar      binary
*.png      binary
*.jpg      binary
*.zip      binary

# Maven Wrapper（注意：mvnw 是 *nix 脚本，LF；mvnw.cmd 是 Windows，CRLF）
mvnw       text eol=lf
mvnw.cmd   text eol=crlf
